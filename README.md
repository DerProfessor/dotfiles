# Professor's dotfiles
Here I share my dotfiles.

## Install
To install dotfiles run the following commands:
```zsh
cd $HOME
git clone https://github.com/DerProfessor/dotfiles.git .dotfiles
cd .dotfiles
./init.sh
```

